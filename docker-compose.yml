version: "3.9"
services:
  bots:
    build: 
      context: ./bots/
      dockerfile: ./Dockerfile
    image: devextralabs/discord-bot
    volumes:
      - ./bots:/usr/src/app/discordbot
    ports:
      - 8501

  job_scheduler:
    build:
      context: ./job_scheduler
      dockerfile: ./Dockerfile
    image: 
      devextralabs/job_scheduler
  
  
  # visualization:
  #   build:
  #     context: ./visualization
  #     dockerfile: ./Dockerfile
  #   image: devextralabs/visualization-web
  #   volumes:
  #     - ./visualization/:/usr/src/app/
  #   ports:
  #     - 8000

    # depends_on:
    #   - bots
    #   - bacalau