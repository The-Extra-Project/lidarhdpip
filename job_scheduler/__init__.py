import sys
import os
"""
microservice that runs the concurrent jobs for the clietns on the bot parallely and gets the status from bacalhau/ECS network.
"""
sys.path.append(os.path.join(os.getcwd(), '../'))